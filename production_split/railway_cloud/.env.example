# Railway Relay Server Configuration
# This key must match the one in your Home Engine .env
INGEST_API_KEY=replace_with_your_secure_random_key

# Optional: Port is usually handled by Railway automatically, 
# but you can set it for local testing
PORT=8000
